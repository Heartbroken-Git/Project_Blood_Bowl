#ifndef __ACTIONBLOCK_HPP__
#define __ACTIONBLOCK_HPP__
/**
 * @file 
 * @author 
 * @since 
 * @brief 
**/

#include <queue>
#include <vector>


class ActionBlock : public Actions{
    public:
    ActionBlock(Player actingPlayer);
    ~ActionBlock();
   	doAction(Player oppoPlayer); //check surrounding, then roll dice(s), then apply changes
    
    private:
    Player actingPlayer;_
};

#endif
